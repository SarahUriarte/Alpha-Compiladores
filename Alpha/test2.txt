let
    var x : int;
    var y : string;
    var n : int
in
begin
    x := 5;
    n := x;
    while x<8 do
    begin
        x := x+1;
        while 3 do
        let
            var m : string
        in
        begin
            let
                var x : string
             in
            if 3 == 3 then
                x := y + "primorosa"
            else
                x := y + "babosa";
            m := "Aloha"
         end;
         x:= x + 2;
         print(x)
    end
end